process.container = 'quay.io/nextflow/bash'

profiles {
    test {
        params.greeting = 'Testing'
    }
    aws {
        process.executor = 'awsbatch'
    }
    local {
        process.executor = 'local'
    }
}
